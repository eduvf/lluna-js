export const TEST = {
    fact: [
        ['st', '#5', '$0'],
        ['st', '#0', '$1'],
        ['op', '<', '#0', '$0'],
        ['jp', '@9'],
        ['op', '+', '$0', '#-1'],
        ['cl', '@1', 'ac'],
        ['op', '*', '$0', 'ac'],
        ['st', 'ac', '$1'],
        ['jp', '@A'],
        ['st', '#1', '$1'],
        ['rt', '$1'],
        ['ht'],
    ],
    fact_iter: [
        ['st', '#5', '$0'],
        ['st', '#1', '$1'],
        ['st', '#1', '$2'],
        ['op', '+', '$0', '#1'],
        ['jp', '@9'],
        ['op', '*', '$1', '$2'],
        ['st', 'ac', '$1'],
        ['op', '+', '$2', '#1'],
        ['st', 'ac', '$2'],
        ['op', '<', '$2', 'ac'],
        ['jp', '@5'],
        ['rt', '$1'],
        ['ht'],
    ],
};

/*

fact:

@x00: st  #5  $0  -> n
@x01: st  #0  $1  -> r
@x02: op  <   #0  $0  -> (< 0 n) -> ac: t_/f_
@x03: jp  @9 
@x04: op  +   $0  #-1  -> if true
@x05: cl  @1  ac
@x06: op  *   $0  ac
@x07: st  ac  $1
@x08: jp  @A 
@x09: st  #1  $1  -> if false
@x0A: rt  $1
@x0B: ht


fact_iter:

@x00:   st  #5  $0  -> n
@x01:   st  #1  $1  -> r
@x02:   st  #1  $2  -> i
@x03:   op  +   $0  #1   -> (+ n 1)
@x04:   jp  @9
@x05:   op  *   $1  $2
@x06:   st  ac  $1
@x07:   op  +   $2  #1
@x08:   st  ac  $2
@x09:   op  <   $2  ac  -> (< i {+ n 1})
@x0A:   jp  @5
@x0B:   rt  $1
@x0C:   ht


*/
